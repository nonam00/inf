p_usl = 1 # условие должно быть тождественно истине или лжи
A = 1 # 0 - наименьшее 1 - наибольшее
# шаблонная
for x in [k * 0.25 for k in range(-10000, 10000)]
    # промежуток
    P = 13 <= x <= 19 
    Q = 17 <= x <= 23
    f = (not ((not (P)) <= (Q))) <= ((A) <= ((not (Q)) <= (P)))
    # Если наибольшее ==
    # Если наименьшее !=
    if f == f_usl:
        print(x)

